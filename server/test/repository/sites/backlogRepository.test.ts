import { search } from "./../../../repository/sites/backlogRepository";
// API通したテストはしない or 指定時のみ動くようにしたい

describe("searchのテスト", () => {
  test("backlogからの検索結果を取得できる", async () => {
    // const firstResult = (await search("test", 1))[0];
    // console.log(firstResult);
    // await expect(firstResult).resolves.toEqual([]);
  });
});
